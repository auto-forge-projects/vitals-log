# DL-12-001: Dockerfile — sıfır bağımlılık, non-root, healthcheck

- Tarih: 2026-08-10
- Faz: 12 — CI/CD
- Mod: AUTOPILOT
- Karar: `node:22-alpine` taban imaj, `npm install` adımı YOK (çalışma zamanı bağımlılığı sıfır — SEC-11), non-root `app` kullanıcısı, `/app/data` volume, bağımlılıksız `/health` healthcheck.
- Değerlendirilen alternatifler: `node:22` (slim değil) — reddedildi, gereksiz imaj boyutu; multi-stage build — reddedildi, `npm install` adımı olmadığı için katma değeri yok (LITE basitlik tercih edildi).
- Gerekçe: Faz 5/7 kararlarının (sıfır bağımlılık, non-root, volume) doğrudan uygulanması.
- Riskler: `MOUNT_PREFIX` deploy secret'ı henüz `deploy.json`'a eklenmedi — Faz 15 teknik borcu.
- Geri alınabilirlik: Yüksek.
- İnsan onayı: Otomatik (AUTOPILOT, `docker build` başarılı — kalite kapısı geçti)
- Varsayım mı?: Hayır.
