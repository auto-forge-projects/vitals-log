# DL-09-001: Development tamamlandı — TDD ile 8 modül, sıfır bağımlılık

- Tarih: 2026-08-10
- Faz: 9 — Development
- Mod: AUTOPILOT
- Karar: Faz 8 planındaki 8 task (derive/validate/db/csv/routes/server/public/entegrasyon) TDD ile (her task için red→green ayrı commit çifti) tamamlandı. Çalışma zamanı bağımlılığı sıfır (`node:http`, `node:sqlite`, `node:test`).
- Değerlendirilen alternatifler: Faz 5'te önerilen Express — reddedildi (mimari `node:http` + Express-uyumlu middleware zincirine karar verdi, DL-05-001); bağımlılık kurulumu Faz 9 ağ erişimi olmadığı için zaten mümkün değildi.
- Gerekçe: Faz 5/7 kararlarının (node:sqlite, capability-URL, CSP, rate-limit) birebir uygulanması; her task kendi kabul kriterine göre test edildi.
- Bulgular/düzeltmeler (TDD sürecinde ortaya çıkan, kapsam dışı değil): (1) `validate.js`'in kendi ürettiği varsayılan `ts` (`new Date().toISOString()`, milisaniyeli) kendi regex'iyle uyuşmuyordu — regex düzeltildi + regresyon testi eklendi. (2) `PUT` handler DB satırındaki `id/created_at/updated_at` alanlarını validate'e sızdırıyordu (allowlist reddi) — düzeltildi. (3) İstemci tarih gösterimi (`formatDateLabel`) sistem TZ'sine bağımlıydı, testte İstanbul dışı TZ'de yanlış saat gösteriyordu — `ts` dizesinden doğrudan ayrıştırmaya çevrildi (TZ-bağımsız).
- Riskler: Rate limit (SEC-4) süreç-içi bellekte (yeniden başlarsa sıfırlanır) — tek kullanıcılı LITE kapsamında kabul edilebilir.
- Geri alınabilirlik: Yüksek (kod, testlerle korunuyor; her modül izole).
- İnsan onayı: Otomatik (AUTOPILOT, `npm test` 68/68 yeşil, coverage %98.05, kalite kapısı geçti)
- Varsayım mı?: Hayır.
